meta {
  name: Delete Product
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/v1/products/:id
  body: none
  auth: none
}

params:path {
  id: {{productId}}
}

headers {
  Authorization: Bearer {{token}}
}